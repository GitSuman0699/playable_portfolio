import 'package:jaspr/dom.dart';

// Theme colors using CSS variables for dynamic theming
const primaryColor = Color.variable('--primary');
const primaryDark = Color.variable('--primary-dark');
const bgPrimary = Color.variable('--bg-primary');
const bgSecondary = Color.variable('--bg-secondary');
const bgCard = Color.variable('--bg-card');
const textPrimary = Color.variable('--text-primary');
const textSecondary = Color.variable('--text-secondary');
const textMuted = Color.variable('--text-muted');
const accent = Color.variable('--accent');
const borderColor = Color.variable('--border-color');

// Breakpoints
const mobileBreakpoint = 768;
const tabletBreakpoint = 1024;
